<h1>Users</h1>

<table>
<thead>
	<tr>
		<th>name</th>
		<th><th>
	</tr>
	</thead>
	<tr>
		<% @users.each do |users| %>
	<tbody>
		<td><%= users.name %></td>
		<td><%= link_to "show", user_path(users.id) %></td>
	</tbody>
</tr>
<% end %>
</<table>